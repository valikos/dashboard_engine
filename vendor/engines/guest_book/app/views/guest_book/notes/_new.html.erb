<%= form_for @new_note, html: {role: :form} do |f| %>

  <div class="form-group">
    <%= f.text_area :description, class: 'form-control' %>
  </div>

  <br />

  <button type="submit" id="submit_note" class="btn btn-primary">Submit</button>

<% end %>