<%= form_for(@demo_look) do |f| %>
  <% if @demo_look.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@demo_look.errors.count, "error") %> prohibited this demo_look from being saved:</h2>

      <ul>
      <% @demo_look.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
